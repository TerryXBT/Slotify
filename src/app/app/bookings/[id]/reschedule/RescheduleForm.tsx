'use client'

export default function RescheduleForm({ bookingId }: { bookingId: string }) {
    return (
        <div className="p-4">
            <p>Reschedule form for booking {bookingId}</p>
        </div>
    )
}
